// flycast_shim/version.h -- placeholder for the desktop <version.h> feature-test
// header (RXDK's CRT predates it). Empty: Flycast only includes it for the
// __cpp_lib_* macros, whose absence falls back to the pre-C++17 code paths.
#pragma once
